services:
  salas:
    build: .
    image: salas:latest
    container_name: salas
    ports:
      - "8000:80"
    depends_on:
      - mariadb
    networks:
      - salas-network
    volumes:
      - ./:/var/www/html
    environment:
      HOME: /tmp
    user: "${UID:-1000}:${GID:-1000}"

  mariadb:
    image: mariadb:11
    container_name: salas_mariadb
    restart: always
    environment:
      MYSQL_DATABASE: salas
      MYSQL_USER: salas
      MYSQL_PASSWORD: salas
      MYSQL_ROOT_PASSWORD: salas
    volumes:
      - mariadb_data:/var/lib/mysql
    networks:
      - salas-network

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: salas_phpmyadmin
    restart: always
    depends_on:
      - mariadb
    ports:
      - "8081:80"
    environment:
      PMA_HOST: mariadb
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: salas
    networks:
      - salas-network
      
  selenium:
    image: selenium/standalone-chrome
    container_name: salas_selenium
    ports:
      - "7900:7900"
    networks:
      - salas-network
    shm_size: 2gb

  # No /etc/hosts do seu computador colocar a linha `127.0.0.1 auth.local` 
  # particularidades do protocolo oAuth1 usado na USP
  senhaunica-faker:
    image: uspdev/senhaunica-faker
    container_name: salas_senhaunica-faker
    ports:
      - "3141:3141"
    environment:
          - APP_URL=http://auth.local:3141
    networks:
      salas-network:
        aliases:
          - auth.local

  mailpit:
    image: axllent/mailpit:latest
    container_name: salas_mailpit
    ports:
      - "1025:1025"   # SMTP
      - "8025:8025"   # Interface Web
    networks:
      - salas-network

networks:
  salas-network:

volumes:
  mariadb_data: